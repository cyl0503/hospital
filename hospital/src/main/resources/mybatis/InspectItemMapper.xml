<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cyl.hospital.mapper.InspectItemMapper">

    <!--查询所有检查项目-->
    <select id="getCheckItemList" resultType="checkInspectItemInfo" >
        select * from checkInspectItemInfo where type = 1
    </select>

    <!--查询所有检验项目-->
    <select id="getInspectItemList" resultType="checkInspectItemInfo" >
        select * from checkInspectItemInfo where type = 2
    </select>

</mapper>